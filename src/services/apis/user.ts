// import { AxiosResponse } from 'axios';

// import { Token, User } from '../../types/user';
// import ApiHandler from './ApiHandler';

const UserApi = {
  // verifyToken: (data: Token): Promise<AxiosResponse<Token>> =>
  //   ApiHandler.get(`/users/${data.userId}/verify/${data.token}/email`, {}, {}),
  // getUserProfile: (): Promise<AxiosResponse<User, any>> =>
  //   ApiHandler.get('/users/me', null, {}),
};
export default UserApi;
